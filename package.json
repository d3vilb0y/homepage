{
  "name": "homepage",
  "version": "1.0.0",
  "private": true,
  "description": "A simple static onepager served by a Cloudflare Worker.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
